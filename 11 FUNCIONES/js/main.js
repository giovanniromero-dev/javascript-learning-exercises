// Sintaxis de desclaración de funciones

function suma (a, b) {
    console.log(a + b);
}

function division (a, b) {
    let c = a / b;
    return c;
}
// Sintaxis de invocación
suma(4,6);

let resultado = division(10,2);
console.log(resultado);